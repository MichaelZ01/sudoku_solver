




def is_valid_grid(grid: list[list[int]]) -> bool:
    '''
    For a sudoku grid to be valid, we must check:
    - Each column has no dups
    - Each row has no dups
    - Each box has no dups
    '''
    # Check columns
    


    # Check rows

    # Check squares